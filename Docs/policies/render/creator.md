# Render Policy: creator

Intent: prioritize quality while remaining practical for typical creation workflows.

Defaults:
- Edge compatibility: auto-resize inputs when dimensions mismatch.
- Auto-resize kernel: bicubic (`resize_bicubic_rgba16f`).

Notes:
- This tier is a good default for most interactive editing/export at 1080p–4K.

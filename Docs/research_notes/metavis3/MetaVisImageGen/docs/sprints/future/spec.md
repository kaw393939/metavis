# MetaVisImageGen - Specification

## Goals
1.  Port the LIGM (Large Image Generation Model) logic.

## Requirements
- **Determinism**: Generation must be reproducible given a seed.
- **Integration**: Must produce assets compatible with `MetaVisSimulation` (e.g., textures, models).

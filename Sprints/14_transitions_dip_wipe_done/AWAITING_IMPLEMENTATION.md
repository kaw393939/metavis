# Awaiting Implementation

## Transition Types
- (done) `TransitionType.dip` implemented in compiler + compositor.
- (done) `TransitionType.wipe` implemented in compiler + compositor.

## Tests
- (done) No-mock E2E tests using `Tests/Assets`:
  - dip midpoint near-black
  - wipe midpoint shows spatial separation
  - assets coverage for `.mov`, `.mp4`, `.exr`, `.fits`, `.vtt`


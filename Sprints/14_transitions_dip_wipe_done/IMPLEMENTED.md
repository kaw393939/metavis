# Implemented Features

## Status: In Progress

## Implemented
- Transition rendering for 2-clip overlaps: `crossfade`, `dip(color)`, `wipe(direction)`
- Metal compositor kernels: `compositor_dip`, `compositor_wipe`
- Engine binding + pipeline caching for `dip`/`wipe` params
- No-mock E2E tests using `Tests/Assets` (MOV/MP4/EXR/FITS/VTT)

